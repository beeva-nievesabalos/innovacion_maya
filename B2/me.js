//console.log('me: idAgent=2');

/*function define(name, value) {
	console.log('define: '+ name +"="+value);

    Object.defineProperty(exports, name, {
        value:      value,
        enumerable: true
    });
}

define("idAgent", 2);

var dbHelper = require('../db/dbHelper');

dbHelper.getURIFromAgent(this.idAgent, function(result){
	define("URI",result);
});

dbHelper.getUserIDFromAgent(this.idAgent, function(result){
	define("idUser",result);
});*/



