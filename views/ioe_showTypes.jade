extends layout

block content
  ul
    each val in result
      li: a(href='./type/' + val.id) #{val.name}

block sidebar
  .widget
    h1= subtitle
    p= info
    p Formats stored in database.
    p